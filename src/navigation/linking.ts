const linking = {
    prefixes: ['rezzapp://' /* your linking prefixes */],
    config: {
        screens: {
            NATIVE_DASHBOARD: {
                path: 'native_dashboard',
            },
        },
    },
};

export default linking;