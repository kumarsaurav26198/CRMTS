import React from "react";
import { withTheme } from "styled-components/native";
import { MainWrapperWhite } from "../../utils/globalStyles";

const AppointmentListSheet = () => {
    return (
        <MainWrapperWhite>

        </MainWrapperWhite>
    )
}

export default withTheme(AppointmentListSheet)